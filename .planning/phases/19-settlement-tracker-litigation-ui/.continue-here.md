---
phase: 19-settlement-tracker-litigation-ui
task: 0
total_tasks: TBD (planning not started)
status: planning
last_updated: 2026-04-05T18:30:00
---

<current_state>
Session 19 has NOT started coding. We spent the session reading both the source settlement tracker app and the portal's existing codebase to understand patterns. Frank corrected the approach — we need to PLAN before coding, and build fresh inside the portal using its existing patterns, NOT import the source app's dependencies/styling.
</current_state>

<completed_work>

- Read full integration plan: `myProjects\_dev-ops\settlement-tracker-integration.md`
- Read all source settlement tracker components (11 files): LitigationFiles.tsx, DisputeLanding.tsx, LitigationDataGrid.tsx, LitigationTabs.tsx, LitigationFileForm.tsx, StepsPanel.tsx, StepsDataGrid.tsx, StepForm.tsx, AttorneyScorecard.tsx, ReferralAnalysis.tsx, LiquidityTab.tsx, FileFinancials.tsx, SettlementCard.tsx, SettlementForm.tsx, StateSelector.tsx, StateRequirementsModal.tsx
- Read portal's HANDOFF.md (the real project doc, not CONTEXT.md which doesn't exist)
- Explored portal's full UI patterns via agent (comprehensive report in context)
- Confirmed Session 18 commit `f25ab10` is on staging, clean working tree
- Confirmed ported types (7 files in src/types/settlement-tracker/) and hooks (11 files in src/hooks/settlement-tracker/) exist and compile
</completed_work>

<remaining_work>

- PLAN the settlement tracker litigation UI before writing any code
- Build `/dashboard/settlement-tracker/page.tsx` — track selector landing + litigation view
- Build all litigation sub-components using portal's native patterns (NO shadcn, NO react-hook-form)
- Add sidebar nav entry for Settlement Tracker
- BINGO required before any code changes
</remaining_work>

<decisions_made>

- Frank's correction: The source settlement tracker repo is REFERENCE ONLY. Do not import its UI patterns, dependencies, or styling. Build fresh using the portal's existing approach.
- Portal UI pattern: inline HTML + Tailwind + CSS variables (--bg-primary, --bg-surface, --text-primary, --accent, etc.). No component library. No react-hook-form. Controlled state forms with useState.
- Portal has no toast system — uses inline status messages or simple alerts
- Portal modals: custom-built fixed overlays with conditional rendering, not a dialog library
- Portal tables: plain HTML tables with inline style objects (thStyle, tdStyle patterns)
- White-label: terminology should be industry-wide for public adjusters, not CCS-specific. "Litigation" / "Settlement" / "Appraisal" / "Mediation" are industry terms (fine to keep). But no CCS branding, no "Coastal Claims" references.
- @tanstack/react-query IS installed (from Session 18) and the hooks use it — this is the one new dependency, already committed
</decisions_made>

<blockers>
- None — just needs planning before execution
</blockers>

<context>
Frank wants to slow down and plan. The key insight is: the source settlement tracker is a polished Vite+React app with shadcn/ui, react-hook-form, and its own auth system. We CANNOT bring that baggage into the portal. We need to understand the BUSINESS LOGIC and DATA from the source, then rebuild the UI from scratch using the portal's native patterns (inline styles, CSS variables, plain HTML tables, useState forms, custom modals).

The hooks are already ported and adapted (Session 18) — they use useSTSupabase() for portal auth and add org_id filtering. The types are ported. The database tables exist. What remains is purely UI work, but it needs to be portal-native UI.

HANDOFF.md (not CONTEXT.md) is the canonical project document. Read it first every session.
</context>

<next_action>
1. Read HANDOFF.md to ground yourself in the portal
2. Present a plan to Frank for the Litigation track UI — what components, what patterns, how it fits the portal's style
3. Get BINGO before writing any code
4. Build incrementally — landing page first, then data grid, then forms/modals, then analytics tabs
</next_action>
